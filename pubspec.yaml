name: git_finder
description: A console application which finds Git repositories with un-commited or un-pushed changes.
version: 1.0.0
repository: https://github.com/laurence-trippen/git_finder
environment:
  sdk: ^3.8.1

dependencies:
  path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
