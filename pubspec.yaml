name: git_finder
description: A console application which finds Git repositories with un-commited or un-pushed changes.
version: 1.0.0
repository: https://github.com/laurence-trippen/git_finder
publish_to: none
environment:
  sdk: ^3.8.1

dependencies:
  path: ^1.8.0
  chalkdart: ^3.1.0
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  build_runner: ^2.7.1
  logging_padding_gen:
    git: https://github.com/laurence-trippen/logging_padding_gen
    # path: /Users/laurencetrippen/Dev/SmartElephantApps/logging_padding_gen
